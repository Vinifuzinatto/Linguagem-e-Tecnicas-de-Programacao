#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
	
	 int a,b,c; //as variáveis são separadas entre vírgulas pra reprensentar os espaços separados que os dados serão gardados na memória.
	 
	 a = 8;
	 b = 19;
	 c = a+b;
	 
	 printf("A soma de %d + %d = %d", a,b,c);
	
	return 0;
}
